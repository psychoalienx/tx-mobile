import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    alignItems: "center",
  },
  button: {
    marginTop: 140,
    width: "100%",
    maxWidth: 342,
  },
});
